rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Games collection - allow read/write for all users (anonymous game access)
    match /games/{gameId} {
      allow read, write: if true;
    }
  }
}
