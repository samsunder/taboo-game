rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access - Firestore is not used, only Realtime Database
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
