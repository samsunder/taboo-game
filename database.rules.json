{
  "rules": {
    "$key": {
      ".read": "auth != null && $key.beginsWith('game:')",
      ".write": "auth != null && $key.beginsWith('game:')",
      ".validate": "newData.isString() && newData.val().length <= 50000"
    }
  }
}
